import styled from 'styled-components';
import { Button as ButtonForm } from '../FormAuth/styles';

export const Container = styled.div`
    width: 100%;
`;

export const Button = styled(ButtonForm)``;
