import { Graphql } from '@styled-icons/simple-icons';
import * as Styled from './styles';

export const Loading = () => {
  return (
    <Styled.ContainerLoading>
      <Graphql />
    </Styled.ContainerLoading>
  );
};
