import styled from 'styled-components';
import { ContainerResults as Container } from '../RegisterUser/styles';

export const ContainerResults = styled(Container)`
  h2 {
    margin-bottom: 20px;
  }
`;
