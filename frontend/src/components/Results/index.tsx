import { Title } from '../Title';
import * as Styled from './styles';

export const Results = () => {
  return (
    <Styled.Container>
      <Title text="Resultado" />
    </Styled.Container>
  );
};
